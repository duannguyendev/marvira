/** Platform global-leaderboard contribution (not creator rewardPoints). */
export const GLOBAL_SCORE_BASE = 50;
export const GLOBAL_SCORE_PER_PLACE = 20;
export const GLOBAL_SCORE_QUESTION_CAP = 200;
export const GLOBAL_SCORE_PER_EVENT_CAP = 300;
export const GLOBAL_SCORE_DAILY_CAP = 1000;

/** Event-only rewardPoints caps (do not affect globalScore). */
export const USER_REWARD_POINTS_MAX = 1000;
export const STAFF_REWARD_POINTS_MAX = 10000;
