================================================================================
MARVIRA — RELEASE CREDENTIALS (OWNER PROVIDES LATER)
================================================================================
Updated: 2026-07-30

PURPOSE
  Developers implement against ENV *names* and placeholders only.
  Fill this file when you are ready (or paste values into the real secret
  stores listed under “Where to put”). Do NOT commit real secrets to git.

HOW TO USE (website first)
  1. Provide section A (marketing URL, support email) + minimal API secrets
     in section B (JWT, DATABASE_URL, REDIS) for WEB-DEPLOY.
  2. After https://www.<domain>/privacy is live → submit Meta App Review.
  3. Provide sections C–E (Google / Apple / Facebook) while mobile auth is coded.
  4. Provide section F (Codemagic / signing) before store builds.

  See perfect_release_checklist.txt Track A → B → C → D.

STATUS legend for each item:
  [ ] PENDING   — not provided yet
  [x] PROVIDED  — set in the secret store (do not paste the secret here)

================================================================================
A. DOMAINS / PUBLIC URLS
================================================================================
>>> PROVIDE THESE FIRST — needed for website deploy + Meta Privacy Policy URL

[x] PRODUCTION_MARKETING_URL
    Value:  https://www.marvira.com
    Where:  mobile MARKETING_SITE_URL
            marketing NEXT_PUBLIC_SITE_URL
            API CORS_ORIGIN
            Meta app → Privacy Policy URL = https://www.marvira.com/privacy
    PROVIDED: 2026-07-31 (owner)

[x] SUPPORT_EMAIL
    Value:  support@marvira.com
    Where:  marketing NEXT_PUBLIC_SUPPORT_EMAIL (build-time)
    PROVIDED: 2026-07-31 (owner)

[ ] PRODUCTION_API_URL
    Recommended: https://api.marvira.com
    How to get: YOU choose this subdomain when you deploy the Nest API.
                1) Create DNS A/CNAME: api.marvira.com → your server / load balancer
                2) Deploy API with HTTPS (Docker / Railway / Fly / AWS / etc.)
                3) Confirm GET https://api.marvira.com/health → 200
    Where:  mobile API_BASE_URL
            dashboard + marketing NEXT_PUBLIC_API_URL
            API CORS_ORIGIN
    Needed for: WEB-DEPLOY (/support, /e/{id}, articles)
    STATUS: PENDING — pick host + point DNS (see “How to get API/JWT/DB/Redis” below)

[ ] PRODUCTION_DASHBOARD_URL
    Recommended: https://admin.marvira.com
    How to get: same as API — deploy Next.js dashboard, DNS admin.marvira.com
    Where:  API CORS_ORIGIN; PASSWORD_RESET_URL base if web reset
    Can follow shortly after marketing if needed

[ ] PRODUCTION_CDN_URL (optional until S3/R2 live)
    Example: https://cdn.marvira.com
    Where:  CDN_URL (API), NEXT_PUBLIC_CDN_URL (dashboard/marketing)

[ ] NEXT_PUBLIC_LEGAL_DRAFT (optional)
    Value: true only while showing yellow counsel banner on /privacy /terms
    Default: unset/false for Meta-facing public site
    Where:  marketing build env

[ ] APP_STORE_URL (after listing exists — NOT required for Meta / web-first)
    Where:  marketing NEXT_PUBLIC_APP_STORE_URL → STORE_READY

[ ] PLAY_STORE_URL (after listing exists — NOT required for Meta / web-first)
    Where:  marketing NEXT_PUBLIC_PLAY_STORE_URL → STORE_READY

================================================================================
HOW TO GET API URL / JWT / DB / REDIS (owner guide)
================================================================================
These are NOT bought from Apple/Google. They come from YOUR hosting setup.

1) PRODUCTION_API_URL (https://api.marvira.com)
   - Rent a VPS or PaaS (Railway, Fly.io, Render, AWS, DigitalOcean, etc.)
   - Deploy marvira_dashboard_api apps/api (see docs/deployment.md + Docker)
   - Point DNS: api.marvira.com → that service (Cloudflare or domain registrar)
   - Enable HTTPS (platform TLS or Cloudflare proxy)
   - Test: curl https://api.marvira.com/health

2) JWT_SECRET
   - Generate yourself (do not reuse demo secrets):
       # PowerShell
       -join ((48..57)+(65..90)+(97..122) | Get-Random -Count 48 | % {[char]$_})
       # or OpenSSL / any password manager “random 48 chars”
   - Put ONLY in API hosting env (never in the mobile app or git)
   - Must be ≥32 random characters

3) DATABASE_URL (PostgreSQL)
   - Create a managed Postgres (Neon, Supabase, Railway, AWS RDS, DigitalOcean, …)
   - Prefer PostGIS if available (optional; app can fall back)
   - Copy the connection string the provider shows, then add pool limit, e.g.:
       postgresql://USER:PASSWORD@HOST:5432/marvira?schema=public&connection_limit=20
   - After first deploy: prisma migrate deploy (entrypoint does this in Docker)

4) REDIS_URL
   - Create Redis (Upstash, Redis Cloud, Railway Redis, ElastiCache, …)
   - Copy URL, e.g. redis://default:PASSWORD@HOST:6379
   - Production MUST have Redis (do not set REDIS_DISABLED=true)

5) CORS_ORIGIN (once URLs exist)
   Example when all three are live:
     https://www.marvira.com,https://admin.marvira.com
   (add more origins if you use staging)

Suggested first deploy set for www.marvira.com:
  NEXT_PUBLIC_SITE_URL=https://www.marvira.com
  NEXT_PUBLIC_SUPPORT_EMAIL=support@marvira.com
  NEXT_PUBLIC_API_URL=https://api.marvira.com
  OAUTH_DEV_BYPASS=false
  JWT_SECRET=<generated>
  DATABASE_URL=<from Postgres provider>
  REDIS_URL=<from Redis provider>
  CORS_ORIGIN=https://www.marvira.com,https://admin.marvira.com

================================================================================
B. API / SERVER SECRETS
================================================================================
Where (typical): hosting env for marvira_dashboard_api / apps/api
See also: marvira_dashboard_api/.env.example
How to create: section “How to get API URL / JWT / DB / Redis” above

[ ] JWT_SECRET
    Rule: 32+ random chars; production boot rejects weak values
    Where: API env
    STATUS: PENDING — generate (see guide above); do NOT paste the real secret here

[ ] DATABASE_URL
    Rule: include connection_limit in prod (e.g. ?connection_limit=20)
    Where: API env
    STATUS: PENDING — create managed Postgres, paste into hosting secrets only

[ ] REDIS_URL  (or REDIS_HOST + REDIS_PORT)
    Rule: REDIS_DISABLED must be unset/false in production
    Where: API env
    STATUS: PENDING — create managed Redis

[ ] OAUTH_DEV_BYPASS=false
    Rule: MUST be false on any public staging/prod
    Where: API env
    Set this when you deploy (value is literally: false)

[ ] CORS_ORIGIN
    Value when ready: https://www.marvira.com,https://admin.marvira.com
    Where: API env

[ ] SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASS / SMTP_FROM
    Needed for: email password-reset for LOCAL users
    Where: API env
    How: use your mailbox provider (Google Workspace SMTP, Resend, SendGrid, …)
         From address can be support@marvira.com or noreply@marvira.com

[ ] PASSWORD_RESET_URL
    Example: https://www.marvira.com/reset-password  (or app deep link)
    Where: API env

[ ] AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / S3_BUCKET / S3_REGION
    Optional: S3_ENDPOINT (R2), CDN_URL
    Where: API env — skip until uploads move off local disk

[ ] SENTRY_DSN (optional)
    Note: referenced in .env.example; wire SDK only if you choose to add it
    Where: API env

================================================================================
C. GOOGLE (Maps + Sign-In + Firebase)
================================================================================

HOW TO GET — Google OAuth client IDs (do this first; ~15–30 min)
----------------------------------------------------------------
1. Open https://console.cloud.google.com/ (use the Google account that owns Marvira)
2. Select project **marvira-biggame** (or create “Marvira” if starting fresh)
3. Menu → APIs & Services → OAuth consent screen
   - User type: External (unless Workspace-only)
   - App name: Marvira
   - Support email: support@marvira.com
   - App domain / Privacy: https://www.marvira.com/privacy (after site is live;
     for now you can save draft and finish domains later)
   - Scopes: email, profile, openid (default)
   - Save
4. APIs & Services → Credentials → Create credentials → OAuth client ID
   Create THREE clients:

   A) Application type: Web application
      Name: Marvira Web
      (Authorized JS origins / redirect URIs can stay empty for mobile ID-token flow)
      → Copy “Client ID”  = GOOGLE_OAUTH_WEB_CLIENT_ID
      → This same value goes to:
           API:   GOOGLE_CLIENT_ID  (or first id in GOOGLE_CLIENT_IDS)
           Mobile: GOOGLE_WEB_CLIENT_ID

   B) Application type: iOS
      Name: Marvira iOS
      Bundle ID: com.marvira
      → Copy Client ID = GOOGLE_OAUTH_IOS_CLIENT_ID
      → Also note “iOS URL scheme” / reversed client id
           (looks like com.googleusercontent.apps.XXXX) for Info.plist

   C) Application type: Android
      Name: Marvira Android
      Package name: com.marvira
      SHA-1: add debug SHA-1 now; add release SHA-1 when you have keystore
      Get debug SHA-1 (PowerShell / keytool):
        keytool -list -v -keystore %USERPROFILE%\.android\debug.keystore -alias androiddebugkey -storepass android -keypass android
      → Copy Client ID = GOOGLE_OAUTH_ANDROID_CLIENT_ID

5. For API production boot, set ONE of:
     GOOGLE_CLIENT_ID=<web-client-id>
   or better (all audiences):
     GOOGLE_CLIENT_IDS=<web-id>,<ios-id>,<android-id>

6. GOOGLE_CLIENT_SECRET (Web client) — optional for our mobile flow; not required now.

WHAT TO SEND BACK (paste into release_credentials.LOCAL.txt, not git):
  GOOGLE_OAUTH_WEB_CLIENT_ID=
  GOOGLE_OAUTH_IOS_CLIENT_ID=
  GOOGLE_OAUTH_ANDROID_CLIENT_ID=
  GOOGLE_REVERSED_CLIENT_ID=   (iOS URL scheme)
  Android debug SHA-1=

[ ] MAPBOX_ACCESS_TOKEN
    How: https://account.mapbox.com/access-tokens/ → create a public token
         (restrict by URL for dashboard, by bundle ID for mobile if desired)
    Where: marvira_mobile Codemagic / .env.local (MAPBOX_ACCESS_TOKEN)
           dashboard: NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN

[x] GOOGLE_OAUTH_WEB_CLIENT_ID     (steps above)
    Value: 563117198235-b0ifoaf81cmgbifs899h81ln1h0i7ebq.apps.googleusercontent.com
    Also use as: API GOOGLE_CLIENT_ID + mobile GOOGLE_WEB_CLIENT_ID
    PROVIDED: 2026-07-31 (owner)
[x] GOOGLE_OAUTH_IOS_CLIENT_ID
    Value: 563117198235-n46g12jl7itkh1535c9hlh8i5u4hrdbe.apps.googleusercontent.com
    REVERSED_CLIENT_ID: com.googleusercontent.apps.563117198235-n46g12jl7itkh1535c9hlh8i5u4hrdbe
    PROVIDED: 2026-07-31 (owner)
[x] GOOGLE_OAUTH_ANDROID_CLIENT_ID
    Value: 563117198235-hc9cc8qqc2e8hncv2bi8hv6pgl75mfns.apps.googleusercontent.com
    Debug SHA-1 (marvira_mobile/android/app/debug.keystore — the one Gradle uses):
      5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25
    Do NOT use ~/.android/debug.keystore (88:F8:…) unless you change signingConfigs.
    Note: Android clients have no client_secret (normal)
    Also: Firebase → Project settings → Android app → add that SHA-1, then
    re-download google-services.json (oauth_client must not stay empty).
    PROVIDED: 2026-07-31 (owner)
[x] Android SHA-1 (debug) — see above
[ ] Android SHA-1 (release later)
[ ] Firebase configs — project already marvira-biggame; re-download if OAuth clients change
    android/app/google-services.json
    ios/Marvira/GoogleService-Info.plist

================================================================================
D. APPLE SIGN-IN
================================================================================

HOW TO GET — APPLE_CLIENT_ID (needs Apple Developer Program, paid)
----------------------------------------------------------------
1. Enroll / sign in: https://developer.apple.com/account
2. Certificates, Identifiers & Profiles → Identifiers
3. Select App ID **com.marvira** (create it if missing: App type, Bundle ID com.marvira)
4. Enable capability: **Sign In with Apple** → Save
5. APPLE_CLIENT_ID for native iOS Sign in with Apple =
     the Bundle ID → **com.marvira**
   Put that exact string in API env: APPLE_CLIENT_ID=com.marvira

6. Xcode / Codemagic: signing team must match; entitlements file already in repo
   (Marvira.entitlements). On a Mac, open project once and confirm “Sign In with Apple”
   capability is on.

7. App Store Connect: create the app record with bundle com.marvira when ready for TestFlight
   (not required just to get APPLE_CLIENT_ID).

WHAT TO SEND BACK:
  APPLE_CLIENT_ID=com.marvira
  (confirm Sign In with Apple is enabled on that App ID)

[ ] Apple Developer Program membership (active)
    STATUS: Renewed 2026-07-31 — waiting for Apple activation email (up to 2 business days)
[ ] App ID com.marvira — capability “Sign In with Apple” enabled
[ ] APPLE_CLIENT_ID=com.marvira
    Where: API env APPLE_CLIENT_ID
    Do after membership is Active

[ ] (If you add web Apple later) Services ID + redirect URLs
    Not required for mobile-only Apple Sign-In

================================================================================
E. FACEBOOK / META LOGIN
================================================================================

HOW TO GET — FACEBOOK_APP_ID + SECRET (+ Client Token)
----------------------------------------------------------------
1. Open https://developers.facebook.com/ → My Apps → Create App
2. Use case: **Authenticate and request data from users with Facebook Login**
   (or “Consumer” / add Facebook Login product)
3. App name: Marvira  · Contact email: support@marvira.com
4. In the app dashboard:
   a) Settings → Basic
      → App ID          = FACEBOOK_APP_ID
      → App Secret      = FACEBOOK_APP_SECRET  (click Show; never put in mobile app)
      → App Domains: marvira.com  (after DNS live)
      → Privacy Policy URL: https://www.marvira.com/privacy  (after marketing live)
      → User Data Deletion: can use support URL or a deletion instructions page
   b) Settings → Advanced / or Security
      → Client Token     = FACEBOOK_CLIENT_TOKEN  (for mobile SDK)
5. Add product **Facebook Login** → Settings
   - Valid OAuth Redirect URIs: can add https://www.marvira.com/ for now
   - For native apps: add iOS bundle com.marvira and Android package com.marvira
     (Facebook Login → Settings → client OAuth / native)
6. Roles → add yourself as admin/developer so you can test in Dev mode
   before Meta App Review.
7. Permissions: request **email** (and public_profile). Public Facebook Login for
   all users needs Meta App Review after Privacy URL is live.

WHAT TO SEND BACK (LOCAL file only):
  FACEBOOK_APP_ID=
  FACEBOOK_APP_SECRET=
  FACEBOOK_CLIENT_TOKEN=

Then we (or you) put:
  API env: FACEBOOK_APP_ID + FACEBOOK_APP_SECRET
  Mobile: FACEBOOK_APP_ID + FACEBOOK_CLIENT_TOKEN
  Android strings.xml / iOS Info.plist placeholders

[x] Meta Developer account + Facebook App
    PROVIDED: 2026-07-31 (owner) — Development mode OK for now
[x] FACEBOOK_APP_ID=1089629620413446
    Where: API env; Android strings; iOS Info.plist; mobile ENV
[x] FACEBOOK_APP_SECRET
    Where: API env ONLY
    STATUS: owner provided in chat 2026-07-31 — store in hosting secrets /
            release_credentials.LOCAL.txt only; DO NOT commit to git
    NOTE: if this chat is shared, reset App Secret in Meta → Settings → Basic
[x] FACEBOOK_CLIENT_TOKEN=64ee43bf2168c7486ae73d3cafe90ea2
    Meta UI label may show as “Application code” under Settings → Advanced → Security
    Where: Android strings.xml; iOS Info.plist; mobile ENV
    PROVIDED: 2026-07-31 (owner)
[ ] Meta App Review — email permission (after https://www.marvira.com/privacy is live)
[ ] Privacy Policy URL in Meta settings = https://www.marvira.com/privacy

================================================================================
OWNER CHEAT SHEET — minimum to unblock API production boot
================================================================================
Provide these ASAP (can be “Development” mode apps):

  [x] GOOGLE_CLIENT_ID=563117198235-b0ifoaf81cmgbifs899h81ln1h0i7ebq.apps.googleusercontent.com
      (Web — 2026-07-31)
  [x] GOOGLE_CLIENT_IDS (recommended for API):
      563117198235-b0ifoaf81cmgbifs899h81ln1h0i7ebq.apps.googleusercontent.com,
      563117198235-n46g12jl7itkh1535c9hlh8i5u4hrdbe.apps.googleusercontent.com,
      563117198235-hc9cc8qqc2e8hncv2bi8hv6pgl75mfns.apps.googleusercontent.com
      (web,ios,android — 2026-07-31)

  [ ] APPLE_CLIENT_ID=com.marvira

  [x] FACEBOOK_APP_ID=1089629620413446
  [x] FACEBOOK_APP_SECRET=(in release_credentials.LOCAL.txt only)
  [x] FACEBOOK_CLIENT_TOKEN=64ee43bf2168c7486ae73d3cafe90ea2

Nice-to-have same day (for mobile buttons to work later):
  GOOGLE_WEB_CLIENT_ID=<same as Web client id>
  FACEBOOK_CLIENT_TOKEN=<from Meta>
  GOOGLE_OAUTH_IOS_CLIENT_ID + reversed client id
  GOOGLE_OAUTH_ANDROID_CLIENT_ID + debug SHA-1

Do NOT commit secrets to git. Use release_credentials.LOCAL.txt (gitignored).

================================================================================
F. MOBILE RELEASE / CODEMAGIC / STORE
================================================================================

[ ] Codemagic account + app linked to this repo
[ ] Codemagic env group (e.g. marvira_mobile_secrets) containing:
      MAPBOX_ACCESS_TOKEN
      API_BASE_URL
      MARKETING_SITE_URL
      (+ Google/Facebook client ids used at build time)
      optional Firebase base64 configs

[ ] Android upload keystore (NOT in git)
    Where: Codemagic signing integration (e.g. marvira_android)
    Local: android/key.properties from key.properties.example

[ ] Apple App Store Connect app record (bundle com.marvira)
[ ] Codemagic iOS code signing / ASC integration (e.g. marvira_asc)
[ ] Play Console app + Data safety form ready
[ ] App Store privacy nutrition labels ready
    Reference: store_privacy_labels.txt (update for Google/Apple/Facebook Sign-In)

================================================================================
G. CONTENT / LEGAL (owner decisions, not “API keys”)
================================================================================

[ ] Launch city / district name
[ ] Counsel-approved Privacy Policy + Terms (mention Google, Apple, Facebook)
[ ] Final support email + company legal entity text for store listings
[ ] Store listing copy + screenshots (or schedule designer)

================================================================================
H. SEED / ADMIN ACCOUNTS (after prod DB exists)
================================================================================

[ ] Rotate or delete seed passwords if prisma seed was run on prod
    Known seeds: admin@marvira.com / demo@marvira.com / staff@marvira.com
[ ] Create real admin account; store password in password manager

================================================================================
FILL-IN TEMPLATE (optional — keep local only, never commit filled copy)
================================================================================

Copy to release_credentials.LOCAL.txt (gitignored) if you want a working sheet:

  PRODUCTION_API_URL=
  PRODUCTION_MARKETING_URL=
  PRODUCTION_DASHBOARD_URL=
  SUPPORT_EMAIL=
  JWT_SECRET=
  DATABASE_URL=
  REDIS_URL=
  MAPBOX_ACCESS_TOKEN=
  NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
  GOOGLE_OAUTH_WEB_CLIENT_ID=
  GOOGLE_OAUTH_IOS_CLIENT_ID=
  GOOGLE_OAUTH_ANDROID_CLIENT_ID=
  APPLE_CLIENT_ID=
  FACEBOOK_APP_ID=
  FACEBOOK_APP_SECRET=
  FACEBOOK_CLIENT_TOKEN=
  SMTP_HOST=
  SMTP_USER=
  SMTP_PASS=
  APP_STORE_URL=
  PLAY_STORE_URL=

Add to .gitignore if you create a filled local copy:
  release_credentials.LOCAL.txt

================================================================================
END
================================================================================
